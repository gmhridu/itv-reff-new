[build]
builder = "dockerfile"

# Your existing configuration
[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmd = "bun install --frozen-lockfile"

[phases.build]
cmd = "bun run build"

[start]
cmd = "bun start"

[variables]
NODE_ENV = "production"
